server:
  port: 8089

spring:
  application:
    name: brewery-eureka

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com.netflix.eureka: off
    com.netflix.discovery: off
